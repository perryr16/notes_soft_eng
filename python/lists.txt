list = ARRAY
-----------
list = ordered and changeable
tuple = ordered and unchangable
set = unordered and unindexed 
dictionary = unordered, changable, and indexed. no duplicates

list1 = ['a', 'b', 'c', 'd']
list1[1] = 'b'
list1[-1] = 'd'
list1[1:3] = ['b', 'c', 'd']
list1[:2] = ['a', 'b', 'c']
list1[2:] = ['c', 'd']
'c' in list1 --> True 
'x' in list1 --> False


list1.append(value) --> adds to end of list
list1.insert(index_pos, value) --> insert value at index pos
list1.remove(value) --> remove value
list1.pop(index) --> removes index. IF NONE GIVEN remove last element
del list1[index] --> remove index pos 
del list1 --> delete whole variable
list1.clear() --> empty the list
new = list1.copy() --> coppies list1, must have assignment
new = list(list)var --> copies list1
c = a + b --> create new list
list1.extend(list2) = adds list2 to end of list1 (not a new var)
list2 = list((1, 2, 3)) --> create new list with vals, need (( ))
sum(list) --> sum of a list

=======

Method	Description
------  -----------
append()  --> 	Adds an element at the end of the list
clear() --> 	Removes all the elements from the list
copy()  --> 	Returns a copy of the list
count() --> 	Returns the number of elements with the specified value
extend()  --> 	Add the elements of a list (or any iterable), to the end of the current list
index() --> 	Returns the index of the first element with the specified value
insert()  --> 	Adds an element at the specified position
pop() --> 	Removes the element at the specified position
remove()  --> 	Removes the item with the specified value
reverse() --> 	Reverses the order of the list
sort()  --> 	Sorts the list