Add File to Git ignore
----------------------

touch .gitignore #create git ignore file in main directory
# add file to .gitignore file (ex: .coverage/, tap_asteroids/__pycache__)
git rm --cached <file> #removes coverage/ from git tracking but not the whole file
git add .gitignore
git commit -m "some git ignore message"
# run a test and see if file (ex: coverage) shows on 'git status'

